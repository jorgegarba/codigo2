function getNumero(parametro:number = 9):void{
  console.log(parametro);
}

function getCadena(numero:number):string{
  return `${numero} en cadena`;
}

function getRaiz(numero:number):number{
  //retornar la raiz cuadrada de un numero
  //sin usar la libreria Math.Sqrt();
  return numero;
}
