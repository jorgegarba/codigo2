function getNumero(parametro) {
    if (parametro === void 0) { parametro = 9; }
    console.log(parametro);
}
function getCadena(numero) {
    return numero + " en cadena";
}
function getRaiz(numero) {
    //retornar la raiz cuadrada de un numero
    //sin usar la libreria Math.Sqrt();
    return numero;
}
